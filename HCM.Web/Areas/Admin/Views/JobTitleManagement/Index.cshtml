@using HCM.Web.Areas.Admin.ViewModels;
@model IEnumerable<JobTitleListModel>

@{
	ViewData["Title"] = "Job Titles Management";
}

<div class="container mt-5">
	<h1 class="text-center mb-4">@ViewData["Title"]</h1>

	<a class="btn btn-success btn mb-2" asp-controller="JobTitleManagement" asp-action="Create">Add Job Title</a>

	<table class="table table-bordered table-hover">
		<caption>Total job titles: @Model.Count()</caption>
		<thead>
			<tr>
				<th>Job Title</th>
				<th>Actions</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var jobTitle in Model)
			{
				<tr>
					<td>@jobTitle.Name</td>
					<td>
						<a asp-area="Admin" asp-action="Edit" asp-controller="JobTitleManagement" asp-route-id="@jobTitle.Id" class="btn btn-info btn-sm">Edit</a>
						<form method="post" asp-action="Delete" asp-controller="JobTitleManagement" asp-area="Admin" class="d-inline ms-2">
							<input type="hidden" name="id" value="@jobTitle.Id" />
							<button type="submit" class="btn btn-danger btn-sm mb-1 mt-1" onclick="return confirm('Are you sure you want to delete this job title?');">Delete</button>
						</form>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>
